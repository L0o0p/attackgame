export class Enemy extends Character {
    constructor(
        characterName,
        mesh,
        animations,
        attributes,
    ) {
        super(characterName, mesh, animations, attributes);
    }

}
